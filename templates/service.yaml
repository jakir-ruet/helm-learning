apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-learning.fullname" . }}
  labels:
    {{- include "helm-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-learning.selectorLabels" . | nindent 4 }}
