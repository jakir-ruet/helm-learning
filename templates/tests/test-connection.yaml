apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-learning.fullname" . }}-test-connection"
  labels:
    {{- include "helm-learning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-learning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
